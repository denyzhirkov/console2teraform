<% if (vpcs && vpcs.length) { %>
<% vpcs.forEach(function(vpc) { %>
resource "aws_vpc" "<%= vpc.resourceName %>" {
  cidr_block = var.<%= vpc.resourceName %>_cidr_block
<% if (vpc.tagsHcl) { %>  tags = {
<%- vpc.tagsHcl %>
  }
<% } %>}
<% }) %>
<% } %> 